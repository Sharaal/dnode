[![dependencies | 1 | 6](https://img.shields.io/badge/dependencies-1%20|%206-blue.svg)](DEPENDENCIES.md)

# Usage

```
const httpAuth = require('@dnode/http-auth')({
  user: process.env.HTTP_AUTH_USER,
  password: process.env.HTTP_AUTH_PASSWORD,
});
```
